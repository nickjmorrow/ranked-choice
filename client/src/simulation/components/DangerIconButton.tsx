// external
import styled from 'styled-components';
import { IconButton } from '~/simulation/components/IconButton';

export const DangerIconButton = styled(IconButton)`
    background-color: ${p => p.theme.dangerColor.cs5};
`;
