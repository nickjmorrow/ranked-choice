// external
import styled from 'styled-components';
import { IconButton } from '~/core/IconButton';

export const CreateIconButton = styled(IconButton)`
    background-color: ${p => p.theme.successColor.cs5};
`;
